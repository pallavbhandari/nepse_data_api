# Dependencies for the optional REST interface (nepse_data_api.api).
# Installs the core library deps plus the web-server stack:
#   pip install -r requirements-api.txt
-r requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
